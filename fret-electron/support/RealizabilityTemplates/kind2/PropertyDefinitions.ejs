<% for (i=0; i < properties.length; i++) {%> 
(* <%-properties[i].fullText -%>  *)
<%var newid = properties[i].reqid.replace(/[^a-zA-Z0-9_~]/g,'')%>
var <%- newid -%> : bool = <%- properties[i].value -%>;  

<%}%>
<% for(i=0; i < properties.length; i++){%> 
<%var newid = properties[i].reqid.replace(/[^a-zA-Z0-9_~]/g,'')%>
<%var reqType = newid.includes('assumption') ? 'assume' : 'guarantee'%> <%- reqType %> <%- newid %>; <%}%>

*)